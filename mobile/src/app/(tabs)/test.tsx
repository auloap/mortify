import { Text } from "react-native";
import { Screen, SectionDesc } from "@/components/ui";

export default function TestTab() {
  return (
    <Screen>
      <SectionDesc>Sin tracker with emotional trigger analysis.</SectionDesc>
      <Text>Full Test tab lands in Phase 3.</Text>
    </Screen>
  );
}
