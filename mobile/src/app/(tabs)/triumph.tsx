import { Text } from "react-native";
import { Screen, SectionDesc } from "@/components/ui";

export default function TriumphTab() {
  return (
    <Screen>
      <SectionDesc>Do goals, Resist goals, and a wins log — every step of faithfulness counts.</SectionDesc>
      <Text>Full Triumph tab lands in Phase 3. Prayer Lock&apos;s card joins it in Phase 4.</Text>
    </Screen>
  );
}
