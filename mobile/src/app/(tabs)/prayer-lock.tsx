import { Text } from "react-native";
import { Screen, SectionDesc } from "@/components/ui";

export default function PrayerLockTab() {
  return (
    <Screen>
      <SectionDesc>Mood tap → AI-generated scripture prayer → real app blocking.</SectionDesc>
      <Text>Lands in Phase 4 (in-app ritual) and Phases 5–6 (real OS-level blocking).</Text>
    </Screen>
  );
}
